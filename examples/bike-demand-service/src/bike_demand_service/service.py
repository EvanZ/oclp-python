"""Future FastAPI application boundary for request-scoped OCLP inference."""

from __future__ import annotations


def create_app() -> object:
    """Reserve the application factory without implying a completed service."""

    raise NotImplementedError(
        "FastAPI inference is the next bike-demand milestone; see README.md."
    )
